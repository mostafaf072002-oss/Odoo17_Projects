<odoo>
    <!-- ================== Tree View ================== -->
    <record id="project_view_tree" model="ir.ui.view">
        <field name="name">project.project.tree</field>
        <field name="model">project.project</field>
        <field name="arch" type="xml">
            <tree string="Projects" multi_edit="1" class="project_table">
                <field name="id"/>
                <field name="name"/>
                <field name="staff_id"/>
            </tree>
        </field>
    </record>

    <!-- ================== Form View ================== -->
    <record id="project_view_form" model="ir.ui.view">
        <field name="name">project.project.form</field>
        <field name="model">project.project</field>
        <field name="arch" type="xml">
            <form string="Project">
                <sheet>
                        <group>
                            <field name="name"/>
                            <field name="staff_id"/>
                        </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- ================== Action ================== -->
    <record id="project_action" model="ir.actions.act_window">
        <field name="name">Projects</field>
        <field name="res_model">project.project</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!-- ================== Menu ================== -->
    <menuitem
        id="project_menu_item"
        name="Projects"
        parent="project_menu"
        action="project_action"
    />
</odoo>