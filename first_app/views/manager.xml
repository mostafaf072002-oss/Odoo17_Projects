<odoo>

    <!-- ================= Window Action ================= -->
    <record id="manager_action" model="ir.actions.act_window">
        <field name="name">Managers</field>
        <field name="res_model">staff.staff</field>
        <field name="domain">[('manager','=',True)]</field>
        <field name="view_mode">tree</field>
    </record>

    <menuitem id="manager_menu_item_view"
              name="Manager"
              parent="staff_menu"
              action="manager_action"
    />


</odoo>