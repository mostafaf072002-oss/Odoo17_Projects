<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_attendance_wizard_form" model="ir.ui.view">
        <field name="name">school.attendance.wizard.form</field>
        <field name="model">school.attendance.wizard</field>
        <field name="arch" type="xml">
            <form string="Mark Class Attendance">
                <group>
                    <field name="classroom_id"/>
                    <field name="date"/>
                    <field name="default_state"/>
                </group>
                <field name="line_ids">
                    <tree editable="bottom">
                        <field name="student_id" readonly="1"/>
                        <field name="state" widget="badge"
                               decoration-success="state=='present'"
                               decoration-danger="state=='absent'"
                               decoration-warning="state=='late'"/>
                        <field name="note"/>
                    </tree>
                </field>
                <footer>
                    <button name="action_confirm" string="Confirm Attendance"
                            type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_attendance_wizard" model="ir.actions.act_window">
        <field name="name">Mark Attendance</field>
        <field name="res_model">school.attendance.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
